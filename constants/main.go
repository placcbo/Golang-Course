package main
import (
	"fmt"
	"math"
)

const s string = "Constant"

func main(){
	fmt.Println(s)
	  const n = 50.00
	    fmt.Println(int64(n))
		a := 500
		b := float64(a)
		fmt.Println(math.Sin(b))
}